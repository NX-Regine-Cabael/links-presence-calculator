# ── Next.js ──────────────────────────────────
/.next/
/out/
next-env.d.ts

# ── TypeScript ───────────────────────────────
*.tsbuildinfo

# ── Node ─────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Local data (personal employee data) ──────
/data/

# ── Tooling / IDE ────────────────────────────
.DS_Store
.env
.env.local
.env.*.local
.vercel

# ── Claude / AI artifacts ────────────────────
# None of the following belongs in version control:
# docs/ contains the PRD, sample Excel files, design specs,
# implementation plans, reference images, and CLAUDE.md.
# Only the Next.js source code is committed.
CLAUDE.md
/docs/
/.superpowers/
